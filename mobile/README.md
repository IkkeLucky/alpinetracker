# mobile app (not started)

Placeholder for the rider/operator Flutter app described in the project brief
(`docs/project-brief.md`): Flutter for MVP speed, with a native Swift/Kotlin bridge for
OS-specific radio/location APIs (notably iOS CoreLocation beacon-region monitoring for the
gear tags, per the brief's connectivity architecture notes).

Not started — the current milestone is the ESP32 bike-unit prototype in `firmware/bike-unit`
reporting to `backend/server`. This directory exists as a placeholder so the repo layout
matches the brief's software stack from day one.
